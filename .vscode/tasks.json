{
	// See https://go.microsoft.com/fwlink/?LinkId=733558
	// for the documentation about the tasks.json format
	"version": "2.0.0",
	"options": {
		"cwd": "${fileDirname}"
	},
	"tasks": [
		{
			"label": "Build extension",
			"type": "shell",
			"command": "${config:io_scene_fbx_patch_ahit.blenderPath} --command extension build",
			"group": "build"
		}
	]
}
